/** Minimal stub so web/SSR can resolve lottie-react-native's optional peer. */
import React from "react";

export const DotLottieReact = React.forwardRef(function DotLottieReact(
  _props: Record<string, unknown>,
  _ref: React.Ref<unknown>,
) {
  return null;
});

export default DotLottieReact;
